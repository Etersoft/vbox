#!/bin/bash

. /etc/sysconfig/vbox/vbox.conf

XAUTHORITY=`mktemp -p /tmp --suffix .xauth`
cp ~/.Xauthority "$XAUTHORITY"
chmod 664 "$XAUTHORITY"
sudo su - "$VBOX_USER" -c "export DISPLAY=\"$DISPLAY\" XAUTHORITY=\"$XAUTHORITY\"; `which VirtualBox`"
rm -f "$XAUTHORITY"

